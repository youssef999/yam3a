import 'package:flutter/material.dart';

// Main App Theme Colors - Based on YAMAA app screenshots (Purple & Orange Theme)
          // Purple gradient background with orange accents
dynamic primaryColor = const Color(0xff5A1E3D);
//const Color(0xFF8B4B89); 
//const Color(0xFFE47B39);         // Orange primary for buttons and accents
dynamic backgroundColor = Colors.grey[100];      // Purple background like screenshots
dynamic appBarColor = const Color(0xff5A1E3D);
//Colors.white;
//const Color(0xFF8E4EC6);          // Purple app bar
dynamic appSurfaceColor = const Color(0xff5A1E3D);
// const Color(0xFF8E4EC6);      // Purple surface color

// Text Colors
dynamic txtColor = Colors.grey[900];                        // White text on purple background
dynamic lightTxtColor = Colors.white70;                 // Light white text

dynamic appBarIconColor = Colors.grey[100];               // Dark icons for white app bar
// Component Colors  
dynamic buttonColor = const Color(0xffE28743);          // Orange buttons like screenshots
dynamic cardColor = Colors.white;                       // White cards and surfaces
dynamic iconColor = const Color(0xff5A1E3D);
//const Color(0xFFE47B39);            // Orange icons to match theme

// Interactive Colors
dynamic clickColor = const Color(0xffE28743);              // Orange click/tap feedback
dynamic shadowColor = Colors.grey[100];                 // Light shadows
dynamic priceColor = const Color(0xffE28743);             // Orange price text

// Status Colors
dynamic successColor = Colors.green;                    // Success states
dynamic failColor = Colors.red;                           // Error states
dynamic ratingColor = const Color(0xFFFFD700);          // Ratings/stars

// Splash Screen Colors - Based on YAMAA purple & orange theme
const Color splashBackgroundStart = Color(0xFF8E4EC6);  // Purple like screenshots
const Color splashBackgroundEnd = Color(0xFF6B2C91);    // Darker purple for gradient
const Color splashTextColor = Colors.white;
const Color splashAccentColor = Color(0xFFEE28743);      // Orange accent
dynamic appDividerColor = Colors.white24;               // Light white dividers on purple    
dynamic appInfoColor = const Color(0xFF8E4EC6);         // Purple info color
dynamic whiteColor = Colors.white;                      // Pure white

// Gradient Colors for Special Effects
const Color gradientStart = Color(0xFF8E4EC6);          // Purple
const Color gradientEnd = Color(0xFF6B2C91);            // Darker purple
const Color primaryGradientStart = Color(0xFFE47B39);   // Orange primary
const Color primaryGradientEnd = Color(0xFFD4621A);     // Darker orange for depth

// Surface and Container Colors
const Color surfaceColor = Colors.white;                // Cards, dialogs
const Color containerColor = Color(0xFFF8FBFF);         // Light containers
const Color dividerColor = Color(0xFFE0E0E0);           // Dividers, borders

// Additional System Colors (for consistency)
const Color warningColor = Color(0xFFF);           // Warning states
const Color infoColor = Color(0xFFE47B39);              // Orange info states matching screenshots
